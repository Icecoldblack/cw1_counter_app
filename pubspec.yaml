name: cw1_counter_app
description: "CSC 4360 Coursework 01: counter with scale-up features, animated image toggle, and Light/Dark theme switching."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/sun.png
    - assets/images/moon.png
